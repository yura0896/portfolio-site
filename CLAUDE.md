# CLAUDE.md — ちばゆうき ポートフォリオサイト

このファイルは、別の Claude アカウントへ作業を引き継ぐためのメモです。
**作業を始める前に最後まで読むこと。** ここに書いた判断は本人と相談して決めたもので、
「よくある構成」に戻すと本人の意図を壊します。

---

## 1. このプロジェクトは何か

- 本人（ちばゆうき）が**自分で使う**ポートフォリオサイト。本人＝依頼者＝運用者
- 本人はアニメーター／イラストレーター。2026年9月にフリーランスとして独立したばかり
- 作風：かわいい絵柄。ちびキャラと女の子のイラストが得意で、よく反応がある
- X：https://x.com/Yukishiro_Yu_Ki（作品はここの画像欄に載っている）
- **作品の元データは別のPCにある**。まだこのフォルダには入っていない

## 2. 本人とのやり取りの仕方

- 日本語で話す。丁寧だが、かしこまりすぎない
- **判断が必要なときは選択肢で聞く**（AskUserQuestion）。本人はこの形式を好む
- 推奨がある場合は、選択肢の1つ目に置いて「（推奨）」と付ける
- 本人が技術に詳しいとは限らない。専門用語を使ったときは短く補足する

---

## 3. 技術構成

- ビルド不要の静的サイト（HTML / CSS / バニラJS）。依存パッケージなし
- 公開先の想定：Cloudflare Pages（手順は README.md の 6 章）
- 作品データとサイト情報は **`assets/js/data.js` に集約**。更新は基本ここだけ
- 作品詳細は `work.html?id=作品ID` の共通テンプレート1枚で全作品をまかなう
- 動画はハイブリッド方式：一覧は軽量ループmp4、詳細は YouTube / Vimeo の埋め込み
- ファイル構成・作品の足し方・素材の仕様は README.md を参照

### このPCの環境で注意すること

- **Node.js も Python も入っていない**（`python` は Windows ストアの空スタブ）。
  このPC上ではローカルサーバーを立てられず、ビルドも走らせられない
- `gh`（GitHub CLI）も入っていない。GitHub のリポジトリ作成はブラウザで本人が行う
- ブラウザペインで `file://` の HTML を開くと静的スナップショット（data: URL）になり、
  **相対パスの CSS / JS / 画像が読み込まれない**。そのままでは表示確認にならない

### 表示確認は Claude 側のクラウド環境でやるのが早い（2026-09-19 に確立）

Claude のクラウド環境には Node と Chromium（Playwright）が入っている。
`device_stage_files` でこのフォルダ一式をクラウドへ吸い上げ、そこで静的サーバーを立てて
Chromium で開けば、**本番と同じ相対パスのまま**表示確認できる。
インライン化ビルドはもう要らない。

```bash
# クラウド側。事前に HTML / css / js / img を全部 stage しておくこと
cd <staged>/portfolio-site && (python3 -m http.server 8731 >/dev/null 2>&1 &)
# あとは Playwright で http://localhost:8731/contact.html などを開いて screenshot
```

- Google Fonts はクラウドのプロキシに塞がれて読めない。`ERR_TUNNEL_CONNECTION_FAILED` は
  検証環境の都合で、本番では出ない。それ以外のエラー・404 は実際の不具合として扱う
- 画像・動画を足した後もこの方法でそのまま確認できる

### ブラウザ側セッションの制約（Claude Code CLI では発生しない）

- ブラウザ側の Claude（claude.ai のプロジェクト）はこのPC上でコマンドを実行する手段
  （device_bash）が無く、git を動かせない。ファイルの読み書きは `device_stage_files` /
  `device_commit_files` で行うため、**変更は作業ツリーに置かれるだけでコミットされない**
- **Claude Code（この CLI）ならこのPC上で直接 git を実行できる**。ブラウザ側からの
  引き継ぎ（画面のスクリーンショット等）を受けたら、変更内容を確認したうえで
  Claude Code 側でまとめてコミットする

---

## 4. デザインの判断（変える前に理由を読むこと）

### 配色は本人のブランドカラーの実測値

- `--accent: #90c0c0`（くすんだ青緑）は、本人の X のヘッダー画像からピクセルを抽出した値。
  画像の 85.9% をこの色が占めていた。推測で選んだ色ではない
- 第2の差し色 `--accent-2: #f7bf94` は**温かいオレンジ**。本人の指示でピンクから変更した
- X のメディア欄はログインが必要で見られなかった。配色はヘッダー画像とアイコンだけが根拠。
  実際の作品を見て色調が違えば調整してよいが、本人に確認してから

### 差し色は「塗り用」と「文字用」を分けている

淡い色をそのまま文字に使うと読めない（`#90c0c0` を白地の文字にすると約 1.9:1）。
そのため同じ色相で濃度だけを変えたトークンに分けている。

| トークン | 用途 |
|---|---|
| `--accent` / `--accent-2` | 塗り・枠線 |
| `--accent-text` | 背景の上に乗る文字（リンク・肩書きなど） |
| `--accent-fg` / `--accent-2-text` | それぞれの塗りの上に乗る文字 |

- **色を変えるときは必ずセットで変更し、コントラストを実測すること**
- 基準は WCAG AA（4.5:1）。現状はライト・ダーク両テーマの全箇所で満たしている（最小 4.75:1）
- 実測値は README.md の 5 章の表にある。変えたら表も更新する
- ダークテーマの値は `@media (prefers-color-scheme: dark)` と `:root[data-theme="dark"]` の
  **2か所に同じものを書いている**。片方だけ直さない

### 「かわいい」方向の構成要素

本人の要望でかわいい方向に振っている。要素の対応表は README.md の 5-3 章。

- フォント：Zen Maru Gothic（丸ゴシック。Google Fonts）
- 角丸 18px、ボタン・タグ・ナビはピル型
- 画像まわりは**白フチ**（`--sticker`）。本人の作品がステッカー風の白フチ表現なのに合わせた
- ヒーローに青緑とオレンジのぼかし円、淡色セクションの上端は 40px の丸み
- プロフィール画像は円形
- ヒーローの見出しは日本語名「ちばゆうき」が主、ローマ字「CHIBA YUUKI」が従

---

## 5. 内容の判断（勝手に戻さないこと）

### 対応できる工程は本人に確認済み。これ以外は載せない

| 群 | 載せるもの |
|---|---|
| アニメーション | 第二原画 / GIFアニメ / 短尺アニメーション |
| イラスト | ちびキャラ / 女の子イラスト / グッズ原画 / アイコン・SNS素材 |
| ソフト | CLIP STUDIO PAINT |

**本人が「できない」「使っていない」と答えたため、意図的に外したもの：**
原画、動画、レイアウト、絵コンテ、撮影、キャラクターデザイン、書籍挿絵、After Effects、Photoshop

- できない仕事を載せると、受注後に本人が困る。「普通は載せるから」という理由で足さない
- サイト内のどこにも（本文・記入例・サンプル作品も含めて）これらを書かない。
  過去に何度か、スキル欄だけ直して他の箇所に残っていたことがある。変更後は全体を grep する
- 動画の書き出し手順は CLIP STUDIO PAINT 前提で書く（After Effects 前提にしない）

### 事実を捏造しない

- 経歴は「2026.9– フリーランスとして活動開始」の1件のみ。独立前の所属や学歴は聞けていない
- 以前はサンプルとして架空の経歴（制作会社勤務など）が入っていたが、事実と違うので削除した
- 対応できる規模（尺・点数）も未確定。`about.html` にコメントで差し替え位置を示してある
- 分からないことは空けておくか本人に聞く。それらしい値で埋めない

### 本人が指示した文言・削除

- キャッチコピー：「かわいいを描いて動かす」
- トップのセクション見出し：「見てほしい作品」（英字ラベル Pick Up）。
  「代表作」は本人が苦手な言い方なので使わない
- About の自己紹介から「大事にしているのは、キャラクターが画面の中でちゃんと息をしている感じです…」
  の段落を本人の指示で削除済み。復活させない
- SNS は X のみ掲載。運用していないアカウントはリンク切れになるので載せない

### 公開される連絡先

- `data.js` の `email` は本人が教えてくれたアドレス。公開後は誰でも見える
- 仕事用に別アドレスを使うかは本人に一度伝えてあるが、明確な返事はまだない

---

## 6. 本人に確認した回答（2026-09-19）

以前の未回答リストは本人に確認済み。**下記は本人の判断なので、勝手に戻さないこと。**

1. **肩書きの順番** → 「Animator / Illustrator」の**まま**で確定。
   入れ替えの提案は本人が見たうえで現状維持を選んだ。もう提案しない
2. **ソフト欄** → CLIP STUDIO PAINT の**1項目のまま**で確定。
   納品形式の併記も欄の削除もしない
3. **有償依頼の導線** → tsunagu.cloud の**プロフィールへの案内のみ**。
   URL は本人から：https://tsunagu.cloud/users/chiba_yuuki97
   - **ワンコイン出品は終了している**（本人談）。「ワンコイン」「少額」「◯円から」等の
     金額に踏み込んだ書き方はしない。プロフィールを見てもらう案内にとどめる
   - `data.js` の `SITE.extraLink` に文言と URL を置き、`contact.html` の
     `data-extra-link` に流し込む
   - 掲載をやめるときは `SITE.extraLink` を `null` にすればブロックごと消える
     （`main.js` 側で親要素ごと削除している）
   - **表示名は「つなぐ」に統一**（本人の指示、2026-09-19）。見出し・本文・ボタンの
     3か所（`extraLink.label` / `note` / `linkText`）は「つなぐ」と書き、
     `tsunagu.cloud` というドメイン表記はリンク先の `url` だけに残す
4. **活動拠点** → 「Japan」のみ。都道府県は出さない（本人の指示）

### まだ答えをもらえていないもの

- 仕事用に別のメールアドレスを使うか。いまは `chibayuuki097@gmail.com` が公開される。
  一度伝えてあるが明確な返事はまだない

## 7. 次にやること

1. ~~本人が掲載作品を選ぶ~~ → 完了（2026-09-20）。11件
   （参加MV2件・GIFアニメ4件・イラスト5件）
2. ~~別PCから作品ファイルを移す~~ → 完了（2026-09-20）。`_source/` に置いてもらい、
   イラストはJPEG圧縮（1600px長辺・85品質基準）、GIFはそのまま `assets/img/` に配置した
3. ~~`data.js` の `WORKS` を実データに置き換え~~ → 完了（2026-09-20）。サンプル作品は削除済み
4. ~~OGP 画像（1200×630）を用意~~ → 完了（2026-09-20）。ヒーローと同じ配色・
   Zen Maru Gothic で新規作成（`assets/img/og-image.jpg`）。
   **`og:image` はまだ相対パスのまま。Cloudflare Pages / 独自ドメインが決まったら絶対URLに差し替える**
5. ~~GitHub にリポジトリ作成・push~~ → 完了（2026-09-19、以降も随時push済み）。
   https://github.com/yura0896/portfolio-site
6. Cloudflare Pages との接続（README.md の 6-3 章）。本人のアカウントでの操作が必要なため、
   ログイン・接続は本人が行う。設定内容の確認とURL決定後の後処理（og:image等）はこちらで対応する
7. 公開前チェックリスト（README.md の 7 章）を消化する

### 参加作品（自分がメインで手掛けていない作品）の載せ方（2026-09-20）

- 本人が参加した作品で、対応外の工程（動画・仕上げなど。5章参照）が含まれる場合、
  **担当欄には対応可能な工程のみ記載し、対応外の工程は書かない**
  （例：第二原画+動画 → 「第二原画」のみ表記。動画のみの参加 → 「参加」とぼかす）
- MVのサムネイルはYouTube動画から本人指定のタイムスタンプのフレームを切り出して使う。
  字幕・ロゴ等が入って不自然な場合は前後のタイミングで撮り直す（本人に確認してから確定）

### 2026-09-20 に追加で判断したこと

- GIFアニメのみで完結する作品（`embed` なし）は、詳細ページで「本編の埋め込みが未設定です」という
  開発者向けメッセージが出てしまう不具合があったため、`main.js` を修正した。
  `category === "animation"` でも `embed` が無ければサムネイル（GIF本体）をそのまま表示する
- イラストの `thumb` は縦長の全身絵が多く、4:3 の中央切り抜きだと顔が切れることが分かったため、
  `style.css` でイラストカードのサムネイルだけ `object-position: top`（上寄せ）にした。
  詳細ページの画像はクロップされず全体表示なので影響はない
- 9件の `summary` / `credit` は本人の意向で空欄にした。空でもレイアウトが崩れないよう、
  `card__summary` と検索結果ページの概要文を、値がある場合だけ表示するよう `main.js` を修正した
- 掲載作品と担当・クライアントの対応：GIFアニメ4件は「担当：GIFアニメ」、イラスト5件は
  「担当：ちびキャラ」、全件「クライアント：個人制作」（本人に確認済みの選び方をそのまま適用）

---

## 8. git の運用

- 署名はリポジトリ内だけに設定済み：`ちばゆうき <chibayuuki097@gmail.com>`（global 設定は触らない）
- コミットメッセージは日本語。1行目に要約、本文に「なぜ変えたか」を書く
- 変更のたびにコミットする。作業ツリーをクリーンな状態で終える

### 2026-09-19 の作業はこのセッション（Claude Code CLI）でコミット済み

前のセッション（ブラウザ側、git を動かせなかった）が作業ツリーに置いた変更と、
そのあと本人が別チャットで指示した「tsunagu.cloud → つなぐ」表示統一を、
このセッションでまとめてコミットした。あわせて空の引き継ぎメモ
（`CLAUDE.md を読んで、続きから進めて.txt`）を削除した。

## 9. 関連リソース（前のアカウントが所有）

以下は前のアカウントのものなので、**このアカウントからは更新できない**。
続きをやるなら、このアカウントで公開し直す。

- デモページ（artifact）：https://claude.ai/artifact/PcEkyGSYg83MbtWRp7KKpN
- 企画書（Docs）：https://claude.ai/code/artifact/3a79e285-f288-4f18-9604-03debebbe0af
  — 目的・想定閲覧者・サイト構成図・素材準備リスト・制作の進め方・公開前チェックリスト。
  本人に共有してもらえば閲覧できる
